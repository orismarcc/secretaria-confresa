# Audit 100% Completion Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete all remaining audit items (A-01, A-07, M-03, B-01..B-09) and consolidate duplicate SEFAZ producers.

**Architecture:** In-place corrections across hooks, components, migrations, and dead-code removal. No new pages or routes needed.

**Tech Stack:** React 18, TypeScript, Supabase PostgREST, TanStack Query v5, Zod, SQL migrations.

---

### Task 1: SEFAZ deduplication — DB migration

**Files:**
- Create: `supabase/migrations/20260526000003_sefaz_deduplicate_producers.sql`

- [ ] Write idempotent SQL: group by normalized CPF + uppercased name, keep earliest `created_at`, re-parent all `sefaz_services` to canonical, delete duplicates, add UNIQUE constraint.
- [ ] Push migration: `echo "Y" | SUPABASE_ACCESS_TOKEN=... npx supabase db push --include-all`
- [ ] Commit.

### Task 2: A-01 — Compensating delete on useCreateProducer failure

**Files:**
- Modify: `src/hooks/useSupabaseData.ts` (useCreateProducer mutationFn)

- [ ] After `producer_demands` insert fails, DELETE the just-inserted producer row as compensation.
- [ ] TypeScript check passes.
- [ ] Commit.

### Task 3: A-07 — Remove dead DataContext.tsx

**Files:**
- Delete: `src/contexts/DataContext.tsx`
- Keep: `src/lib/storage.ts` (still used by `src/lib/imageStorage.ts`)

- [ ] Confirm DataProvider/useData are never referenced outside DataContext.tsx (confirmed: zero consumers).
- [ ] Delete file.
- [ ] TypeScript check passes.
- [ ] Commit.

### Task 4: M-03 — Extract shared analytics utilities

**Files:**
- Create: `src/lib/analyticsUtils.ts`
- Modify: `src/pages/DashboardPage.tsx`, `src/pages/AnalyticsPage.tsx`, `src/pages/ServicesPage.tsx`

- [ ] Extract `getPatrulhaIds`, `getCompletionRate`, `getSettlementStats` to analyticsUtils.ts.
- [ ] Replace inline computations in the three pages with imports.
- [ ] TypeScript check passes.
- [ ] Commit.

### Task 5: B-01 — CPF digit-check validation in ProducerForm

**Files:**
- Modify: `src/components/forms/ProducerForm.tsx`

- [ ] Add `validateCpf(cpf: string): boolean` using the standard modulo-11 algorithm.
- [ ] Add `.refine(validateCpf, 'CPF inválido')` to the Zod `cpf` field.
- [ ] TypeScript check passes.
- [ ] Commit.

### Task 6: B-02/B-05 — Explicit nullsFirst + B-03 better toast messages

**Files:**
- Modify: `src/hooks/useSupabaseData.ts`

- [ ] `usePendingServices`: add `{ nullsFirst: false }` explicitly to position order.
- [ ] Improve generic onError toasts in key mutations: useCreateProducer, useUpdateProducer, useDeleteService.
- [ ] Commit.

### Task 7: B-04 — Add skeletons to tables that lack them

**Files:**
- Modify: as found

- [ ] Check which pages lack skeletons and add them consistently.
- [ ] Commit.

### Task 8: B-08/B-09 audit findings correction + docs update

**Files:**
- Modify: `docs/audit-fixes-status.md`

- [ ] Mark B-08 as "não aplicável — FinalizeServiceModal IS used in OperatorPage.tsx".
- [ ] Mark B-09 as "não aplicável — storage.ts only imports from @/types, no circular dep".
- [ ] Commit docs update.

### Task 9: Final TypeScript + build verification

- [ ] `npx tsc --noEmit` → clean.
- [ ] Update audit score table in docs.
- [ ] Commit.
